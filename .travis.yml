language: php

os:
  - linux
  # PHP is no supported on OS X: https://github.com/travis-ci/travis-ci/issues/2313
  #- osx

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - nightly

script:
  - php generate < travis-input.txt
  - [ -e testt.dat ] && echo "File test.dat exists" || (echo "File test.dat not exists!!" && check_preconditions && actually_build)
